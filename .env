# ── Tradier real-time options data ───────────────────────────────────────────
# Get your token at: tradier.com → Profile → API Access
# With a brokerage (or paper) account this gives real-time OPRA options data.
TRADIER_TOKEN=SPZJk6yhSFgQv0FSnRA717HsGyS1

# ── Database ──────────────────────────────────────────────────────────────────
# DATABASE_URL=sqlite:///trading_journal.db   # default — no need to set

# ── Auth ──────────────────────────────────────────────────────────────────────
# JWT_SECRET=change-me-in-production

# ── AI Assistant ──────────────────────────────────────────────────────────────
# Get your key at: platform.openai.com/api-keys
# Used by /api/ai/chat (server-side only — never exposed to the browser)
OPENAI_API_KEY=
